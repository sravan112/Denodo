CREATE OR REPLACE VIEW dv_test_cassandra AS SELECT bv_test_cassandra.species AS species, bv_test_cassandra.average_size AS average_size, bv_test_cassandra.common_name AS common_name, cast('int', bv_test_cassandra.population) AS population FROM bv_test_cassandra;

ALTER VIEW dv_test_cassandra
 LAYOUT (bv_test_cassandra = [20, 20, 200, 106]);

